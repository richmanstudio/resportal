# Deployment

MVP deployment target is a single VPS with Docker Compose, Nginx, PostgreSQL, Redis, MinIO, the API container and the frontend build served as static assets.

Kubernetes and microservices are intentionally out of scope for MVP.
